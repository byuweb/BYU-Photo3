@model IEnumerable<MvcPhoto2.Models.folderCategory>

@{
    ViewBag.Title = "Index";
   
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @Html.ActionLink("Create Years Folders", "createYears")
</p>
<table>
    <tr>
        <th>
            Category Name
        </th>
        
        <th>
            Path
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    string test = @"file:\\babybubba.unicom.byu.edu\d$\inetpub\wwwroot\photo2" + item.Url;
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.categoryName)
        </td>
        <td>
            <a href="@test">@Html.DisplayFor(modelItem => item.Url)</a>
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.categoryId }) |
            @Html.ActionLink("Details", "Details", new {  id=item.categoryId }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.categoryId })
        </td>
    </tr>
}

</table>
