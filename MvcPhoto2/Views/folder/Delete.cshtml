@model MvcPhoto2.Models.folderCategory

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this? If you delete this folder it will also delete files and folders contained here.</h3>
<fieldset>
    <legend>folderCategory</legend>

    <div class="display-label">categoryName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.categoryName)
    </div>

    <div class="display-label">categoryId</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.categoryId)
    </div>

    <div class="display-label">parentId</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.parentId)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
